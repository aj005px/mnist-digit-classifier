# mnist-digit-classifier
A beginner-friendly handwritten digit classifier built with TensorFlow and Keras. Trains a simple neural network on the MNIST dataset to recognize digits (0–9), evaluates accuracy, and visualizes results using a confusion matrix heatmap.
